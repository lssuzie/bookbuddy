---
name: bookbuddy
description: "AI 读书伴侣 Skill - 把文本转换为有声书。支持内置音色、声音设计、声音克隆、AI 代读播客。"
---

# BookBuddy · AI 读书伴侣 Skill

> 把读书变成愉悦、享受、沉浸的体验。

## 核心原则：主动引导，自动选声

用户来找 BookBuddy，通常不知道能做什么、选什么声音。**你的任务是先识别场景，再自动匹配声音，帮用户完成从"文字"到"听觉"的完整转换。**

---

## 第一步：识别场景

用户说了一句话，你判断属于哪个场景：

| 用户说 | 场景 | 自动匹配声音 |
|:-------|:-----|:------------|
| "帮我读这本书" / "把这篇转成有声书" | 📚 **知识阅读** | `温柔私语` 或 `白桦` |
| "读个故事给我听" / "读这本小说" | 📖 **小说/网文** | `苏打`（有活力有演绎感）或描述想要的戏剧感声线 |
| "Karpathy 最近说了什么" / "总结这篇做成播客" | 🎙️ **播客** | `苏打`（清晰有活力）或 `白桦`（沉稳专业） |
| "帮我学英语" / "读这篇英文" | 🗣️ **学英语** | `Mia` 或 `Chloe` |
| "睡前想听点东西" / "带我做个冥想" | 😴 **放松助眠** | `冰糖` 或 `睡前催眠` |

> **不需要问用户选什么声音** — 根据场景自动匹配。用户不满意可以换。

---

## 第二步：按场景执行

### 📚 知识阅读（论文、教材、非虚构）

**流程：**
1. 确认文件（用户上传或指定路径）
2. 自动选声：`温柔私语`（默认灵魂声线）或 `白桦`（沉稳磁性）
3. 调用 TTS
4. 返回音频路径 + 时长

**命令：**
```bash
python3 <skill>/scripts/generate_audio.py <文件> --voice-design "温柔私语"
```

### 📖 小说/网文（文学、故事、网文）

**小说需要演绎感**，不是平淡朗读。建议用活力的声线或自定义戏剧感声线：

**流程：**
1. 确认文件
2. 自动选声：`苏打`（清爽活力，有演绎张力）
3. 如果用户想要更戏剧化：用声音设计描述想要的声线
4. 调用 TTS
5. 返回音频

**命令：**
```bash
# 用苏打（有活力）
python3 <skill>/scripts/generate_audio.py <文件> --voice 苏打

# 或描述想要的戏剧感声线
python3 <skill>/scripts/generate_audio.py <文件> --voice-design "有戏剧张力的男声，适合朗读小说，能演绎不同角色的情绪"
```

### 🎙️ 播客（先总结再生成）

**用户说"听听 Karpathy 的最新观点"，你要做：**

1. **获取内容**：搜索 / 爬取 / 读取相关内容
2. **口语化总结**：用讲故事的方式写出来
   - 先讲故事本身（发生了什么）
   - 再给评价（好不好、值不值得关注）
   - 300-500 字，短句为主，像朋友聊天
3. **自动选声**：`苏打`（清晰有活力，适合内容输出）或 `白桦`（沉稳专业）
4. **保存总结文本**
5. **调用 TTS**：把文本转成播客音频
6. **告知结果**：音频路径 + 时长

**命令：**
```bash
# 先把内容总结成播客文本 → 保存为 podcast.md
# 再调用 TTS
python3 <skill>/scripts/generate_audio.py podcast.md --voice 苏打
```

**完成标准：**
- ✅ 播客文本已生成（口语化，非分析报告）
- ✅ 音频已生成
- ✅ 告知用户路径和时长

### 🗣️ 学英语（英文内容学习）

**流程：**
1. 确认英文内容
2. 自动选声：`Mia`（美式女声）或 `Chloe`（英式女声）
3. 调用 TTS
4. 返回音频

**命令：**
```bash
python3 <skill>/scripts/generate_audio.py <英文文件> --voice Mia
```

### 😴 放松助眠（睡前 + 冥想）

**流程：**
1. 确认内容（或推荐短篇散文、诗歌、冥想引导文本）
2. 自动选声：`冰糖`（清亮甜美）或 `睡前催眠`（温和缓慢）
3. 调用 TTS（建议慢速 / 极慢速）
4. 返回音频

**命令：**
```bash
python3 <skill>/scripts/generate_audio.py <文件> --voice-design "睡前催眠"
```

---

## 第三步：用户不满意，再让用户选

如果用户说"换一个声音"：

```bash
# 列出可用选项
可用内置音色：冰糖（甜美）/ 茉莉（温柔）/ 苏打（活力）/ 白桦（沉稳）
可用声音设计：温柔私语 / 睡前催眠 / 深夜电台 / 温柔叙述 / 冥想引导 / 悬疑小说

# 让用户选
python3 <skill>/scripts/generate_audio.py <文件> --voice 茉莉
# 或
python3 <skill>/scripts/generate_audio.py <文件> --voice-design "深夜电台"
```

---

## 声音速查

### 场景 → 声音映射

| 场景 | 首选 | 备选 |
|:-----|:-----|:-----|
| 📚 知识阅读 | 温柔私语（默认灵魂声线） | 白桦 |
| 📖 小说/网文 | 苏打（有活力有演绎感） | 自定义戏剧感声线 |
| 🎙️ 播客 | 苏打（清晰有活力） | 白桦（沉稳专业） |
| 🗣️ 学英语 | Mia（美式）/ Chloe（英式） | — |
| 😴 放松助眠 | 冰糖 | 睡前催眠 |

### 内置音色

| 名称 | 语言 | 感觉 |
|:-----|:-----|:-----|
| 冰糖 | 中文 | 清亮甜美 |
| 茉莉 | 中文 | 温柔知性 |
| 苏打 | 中文 | 清爽活力 |
| 白桦 | 中文 | 沉稳磁性 |
| Mia | 英文 | Bright |
| Chloe | 英文 | Warm |
| Milo | 英文 | Deep |
| Dean | 英文 | Calm |

### 声音设计预设

| 名称 | 声线 | 适合 |
|:-----|:-----|:-----|
| **温柔私语** 👑 | 温暖沙哑，贴耳私语 | 一切——BookBuddy 灵魂声线 |
| **睡前催眠** | 温和缓慢，越来越轻 | 放松助眠 |
| **深夜电台** | 磁性醇厚，亲切不亲密 | 散文随笔 |
| **温柔叙述** | 柔和亲切 | 一般叙述 |
| **冥想引导** | 空灵安宁，中性偏柔 | 冥想 |
| **悬疑小说** | 沉稳男中音，神秘悬念感 | 悬疑推理 |

---

## 参考文档

- [CLI 参考](references/skill-cli-reference.md)
- [音色指南](references/skill-voice-guide.md)
- [声音克隆指南](references/skill-voice-clone.md)
- [故障排除](references/skill-troubleshooting.md)

---

## 伦理承诺

> **声音**：只使用你自己的声音，或已获授权的声音参考
> **内容**：只朗读你有权访问的文本
> **禁止**：冒充他人、商业盈利

---

> [!WARNING]
> 本技能仅限个人学习、研究与技术交流使用，严禁用于任何商业用途。